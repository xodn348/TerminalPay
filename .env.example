# API key the agent presents to `termpay pay` (Bearer token).
TERMPAY_API_KEY=ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Per-charge CVV. If unset and a TTY is attached, `termpay pay` prompts.
# Never persisted to disk. Wiped from memory after each authorization.
TERMPAY_CARD_CVV=
